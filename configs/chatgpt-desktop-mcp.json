{
  "mcpServers": {
    "superlocalmemory-v2": {
      "command": "python3",
      "args": ["{{INSTALL_DIR}}/mcp_server.py"],
      "cwd": "{{INSTALL_DIR}}",
      "env": {
        "PYTHONPATH": "{{INSTALL_DIR}}"
      },
      "description": "SuperLocalMemory V2 - Local memory for ChatGPT"
    }
  }
}
