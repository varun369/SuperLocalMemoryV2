---
import { FadeIn, MagneticButton } from '../ui/Motion';
import { ArrowRight, GithubIcon, Terminal } from 'lucide-react';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20">
  <!-- Background Asset -->
  <div class="absolute inset-0 z-0">
    <img 
      src="/assets/hero_neural_mesh_bg_1771269692417.png"
      alt="Neural Network Background"
      class="w-full h-full object-cover opacity-60 mix-blend-screen"
      width="1920"
      height="1080"
    />
    <div class="absolute inset-0 bg-gradient-to-b from-[var(--color-bg-main)]/80 via-transparent to-[var(--color-bg-main)]"></div>
  </div>

  <div class="container-custom relative z-10 grid lg:grid-cols-2 gap-12 items-center">
    <!-- Left: Typography -->
    <div class="space-y-8">
      <FadeIn client:load delay={0.2}>
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 text-sm text-[var(--color-primary)] font-mono mb-4 backdrop-blur-md">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-sky-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-sky-500"></span>
          </span>
          v2.7 is now available
        </div>
        
        <h1 class="text-6xl md:text-7xl font-bold tracking-tight leading-tight">
          Your AI Finally <br />
          <span class="text-gradient">Remembers You</span>
        </h1>
      </FadeIn>

      <FadeIn client:load delay={0.4}>
        <p class="text-xl text-[var(--color-text-muted)] max-w-lg leading-relaxed">
          The only free, local-first memory system for LLMs. Stop re-explaining your codebase. 
          <span class="text-white font-medium">100% Private. Zero Setup.</span>
        </p>
      </FadeIn>

      <FadeIn client:load delay={0.6} className="flex flex-wrap gap-4">
        <MagneticButton client:load>
          <a href="#quickstart" class="glass-button bg-[var(--color-primary)]/10 border-[var(--color-primary)]/20 text-[var(--color-primary)] hover:bg-[var(--color-primary)]/20 flex items-center gap-2 group">
            <Terminal className="w-5 h-5" />
            <span>Install CLI</span>
            <ArrowRight className="w-4 h-4 transition-transform group-hover:translate-x-1" />
          </a>
        </MagneticButton>
        
        <MagneticButton client:load>
          <a href="https://github.com/varun369/SuperLocalMemoryV2" target="_blank" rel="noopener noreferrer" class="glass-button flex items-center gap-2">
            <GithubIcon className="w-5 h-5" />
            <span>Star on GitHub</span>
          </a>
        </MagneticButton>
      </FadeIn>
    </div>

    <!-- Right: Visual (simulated Terminal or Abstract Data) -->
    <div class="relative hidden lg:block perspective-1000">
       <FadeIn client:load delay={0.8} direction="left">
         <div class="glass-panel p-1 rounded-xl transform rotate-y-12 rotate-x-6 hover:rotate-0 transition-transform duration-700 ease-out">
            <div class="bg-[#0d1117] rounded-lg overflow-hidden shadow-2xl border border-white/5">
                <div class="flex items-center gap-2 px-4 py-3 bg-[#161b22] border-b border-white/5">
                    <div class="flex gap-1.5">
                        <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
                        <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
                        <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
                    </div>
                    <div class="text-xs text-gray-500 font-mono ml-2">superlocalmemory — zsh</div>
                </div>
                <div class="p-6 font-mono text-sm leading-relaxed text-gray-300">
                    <div class="flex gap-2">
                        <span class="text-green-400">➜</span>
                        <span class="text-blue-400">~</span>
                        <span>supermemory start</span>
                    </div>
                    <div class="mt-2 text-gray-500">Initializing Cortex...</div>
                    <div class="mt-1 text-gray-500">Loading Local Knowledge Graph...</div>
                    <div class="mt-1"><span class="text-green-400">✔</span> Foundation Model Connected</div>
                    <div class="mt-1"><span class="text-green-400">✔</span> 12,408 Memories Indexed</div>
                    <div class="mt-4 border-l-2 border-blue-500/50 pl-3">
                        <span class="text-blue-400">@Agent</span> I recall you were working on the 
                        <span class="text-yellow-200">AuthModule</span> refactor yesterday.
                        Here are the relevant files...
                    </div>
                    <div class="mt-4 flex gap-2 animate-pulse">
                        <span class="text-green-400">➜</span>
                        <span class="w-2 h-5 bg-gray-500 block"></span>
                    </div>
                </div>
            </div>
         </div>
       </FadeIn>
    </div>
  </div>
</section>
